# updates-dist

AI Vault 更新分发仓库（软件更新 updates.json + 内置资产 builtin-assets.json）。

- 目录结构：`aivault/updates.json`（软件更新清单）、`aivault/builtin-assets.json`（内置资产清单）
- 安装包发布在 Releases（tag 带 `aivault-` 前缀）
- 由 AI-Vault 仓库的 Release CI 自动推送
